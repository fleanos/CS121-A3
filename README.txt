Creating the Index - IndexMain.py:
Changing the path to the dataset zip file where necessary 
in if __name__ == "__main__": e.g. string path going into buildIndex function,
Run the IndexMain.py script

Searching - searchMain.py:
After creating the index,
Run the searchMain.py script,
Launching a local GUI,
Type Query into text entry line,
Press Search button